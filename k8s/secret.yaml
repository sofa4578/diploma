
# REDIS_AUTH підставляється через envsubst у CI/CD pipeline
apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
  namespace: prod-apps
type: Opaque
stringData:
  REDIS_PASSWORD: "${REDIS_AUTH}"
